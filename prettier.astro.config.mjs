// Oxfmt does not support .astro; this config is only used by the Astro-only scripts.
export default {
  plugins: ['prettier-plugin-astro'],
  printWidth: 92,
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
}
